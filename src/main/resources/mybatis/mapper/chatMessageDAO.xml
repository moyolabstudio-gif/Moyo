<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper 
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.springboot.project.dao.IchatMessageDAO">

    <insert id="insertMessage" parameterType="com.springboot.project.dto.chatMsgDTO">
        INSERT INTO CHAT_MSGS (CR_MSG_ID, ROOM_ID, SENDER_ID, CONTENT, SEND_DATE)
        VALUES (SEQ_MSG_ID.NEXTVAL, #{roomId}, #{senderId}, #{content}, SYSDATE)
    </insert>

    <select id="selectMessageList" parameterType="Long" resultType="com.springboot.project.dto.chatMsgDTO">
        SELECT m.CR_MSG_ID, m.ROOM_ID, m.SENDER_ID, u.USER_NAME as senderName, m.CONTENT, m.SEND_DATE
        FROM CHAT_MSGS m
        JOIN USERS u ON m.SENDER_ID = u.USER_ID
        WHERE m.ROOM_ID = #{roomId}
        ORDER BY m.SEND_DATE ASC
    </select>

</mapper>